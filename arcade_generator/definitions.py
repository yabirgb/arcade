# Python dictionary where the keys are the folder description and the
# values are the folder path
required_folders = {
    "content": "content",
    "static": "static",
    "public": "public",
    "themes": "themes",
    "themes/baseline": "themes/baseline"
}
